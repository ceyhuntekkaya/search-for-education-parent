spring:
  profiles:
    active: auto-db-create
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: false
        format_sql: false  # Bunu ekleyin
        se_sql_comments: false  # Bunu ekleyin
        transaction:
          jta:
            platform: org.hibernate.engine.trans1"action.jta.platform.internal.NoJtaPlatform
  datasource:
    dbName: search_edu_app
    #dbUrl: jdbc:postgresql://localhost:5432/ #3
    #dbUrl: jdbc:postgresql://postgres:5432/
    dbUrl: jdbc:postgresql://localhost:5433/ #3

    url: ${spring.datasource.dbUrl}${spring.datasource.dbName}
    username: genixo
    password: genixo
    driverClassName: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: MyHikariCP
      transaction-isolation: TRANSACTION_READ_COMMITTED
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true

  flyway:
    enabled: true
    url: ${spring.datasource.dbUrl}${spring.datasource.dbName}
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
    schemas: public

  rabbitmq:
    username: GenUser2025
    password: Gen114765Str!
    addresses: localhost:5672

  # Multipart ayarlar? buraya ta??nd?
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB        # Maksimum dosya boyutu
      max-request-size: 100MB     # Maksimum request boyutu
      file-size-threshold: 2KB   # Bellek eşiği

management:
  endpoints:
    web:
      base-path: /actuator

server:
  servlet:
    context-path: /api
  # Tomcat ayarlar? eklendi
  tomcat:
    max-swallow-size: 1GB
    max-http-form-post-size: 1GB
logging:
  level:
    root: INFO
    com.genixo.education.search: INFO
    org.springframework: WARN
    org.hibernate: WARN
    org.hibernate.SQL: OFF
    org.hibernate.type.descriptor.sql.BasicBinder: OFF
    org.hibernate.orm.jdbc.bind: OFF

storage:
  base: /app/uploads/
  #base: /Users/macpro/Desktop/file/

sendgrid:
  api:
    key: ${SENDGRID_API_KEY:SG.your-api-key-here}
  from:
    email: ${SENDGRID_FROM_EMAIL:your-verified-email@domain.com}
    name: ${SENDGRID_FROM_NAME:Your App Name}



application:
  security:
    jwt:
      secret-key: uLO6QrqPEmyHZfU1jR3vS8wX2nKgT4iN9bAcDhMkVpYe7tWsJxG5aFdB0CzI4nPmQvUyL8kR3wT9
      expiration: 31536000000
      refresh-token:
        expiration: 31536000000

# SpringDoc/Swagger konfigürasyonu
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  # Önemli: packages-to-scan ile tarama alanını sınırlayın
  packages-to-scan: com.genixo.education.search
  # Default response'ları devre dışı bırak
  default-produces-media-type: application/json
  default-consumes-media-type: application/json
  # Hata durumlarında daha detaylı log için
  show-actuator: false

# Ollama Configuration
ollama:
  api:
    url: https://chat.studyscore.ai/ollama/api/chat
  model: qwen2.5:7b
  context:
    max-tokens: 28000  # qwen2.5 için güvenli limit
    history-messages: 20  # Son 20 mesajı tut